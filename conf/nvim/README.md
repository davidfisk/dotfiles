# david's neovim config

## requirements

Requires Neovim > 0.12. This config no longer users Lazy and uses Neovim's internal package manager. Install these dependences with your favorite package manager. 

- `chafa` - required for rendering dashboard images. 
- `fd` - required for various snacks.nvim Find functions.
