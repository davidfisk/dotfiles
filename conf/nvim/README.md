# david's neovim config

## requirements

Requires Neovim > 0.12. This config no longer uses Lazy and uses Neovim's internal package manager. Install these dependencies with your favorite package manager or build from source if not available. 

- `chafa` - required for rendering dashboard images. 
- `fd` - required for various snacks.nvim Find functions.
